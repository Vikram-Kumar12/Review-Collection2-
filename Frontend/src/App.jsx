import React from "react";
import ReactRouters from "./utils/ReactRouters";

const App = () => {
  return (
    <div style={{fontFamily:'font3'}} className="w-full min-h-screen bg-black text-white">
      <ReactRouters/>
    </div>
  );
};

export default App;
